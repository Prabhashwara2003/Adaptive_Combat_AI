using UnityEngine;
using Unity.MLAgents; // This line tests if ML-Agents is installed

public class InstallationTest : MonoBehaviour
{
    void Start()
    {
        Debug.Log("ML-Agents is installed correctly!");
    }
}